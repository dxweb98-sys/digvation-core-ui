import type { Incident, IncidentStatus, Deployment, OperationalAction } from '../types/operations'; export interface OperationsDataSource { getIncidents():Promise<Incident[]>; getDeployments():Promise<Deployment[]>; getActions():Promise<OperationalAction[]>; transitionIncident(id:string,status:IncidentStatus,reason:string):Promise<Incident>; requestAction(input:Omit<OperationalAction,'id'|'status'|'requestedAt'|'createdAt'|'updatedAt'>):Promise<OperationalAction>; }
