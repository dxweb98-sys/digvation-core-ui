import type { ClientMonitoringDataSource } from './client-monitoring-data-source';
import type { ClientIncident, ClientMonitoringContext, ClientSystem } from '../types/client-monitoring';

type ClientMonitoringRecord = { context: ClientMonitoringContext; systems: ClientSystem[]; incidents: ClientIncident[] };
export const CLIENT_A_CONTEXT: ClientMonitoringContext = { clientId: 'client-nova', displayName: 'Nova Salon', permittedInstallationIds: ['installation-nova-pos-production', 'installation-nova-workshop-staging'] };
export const CLIENT_B_CONTEXT: ClientMonitoringContext = { clientId: 'client-poseidon', displayName: 'Poseidon Filter', permittedInstallationIds: ['installation-poseidon-web-production'] };
const RECORDS: Record<string, ClientMonitoringRecord> = {
  [CLIENT_A_CONTEXT.clientId]: { context: CLIENT_A_CONTEXT, systems: [{ id: 'installation-nova-pos-production', productName: 'Digvation POS', installationName: 'Nova POS Production', environment: 'PRODUCTION', applicationUrl: 'https://pos.novasalon.digvation.id', health: 'HEALTHY', serviceSummary: 'All client-visible services healthy', lastObservedAt: '2026-09-07T01:20:00Z', maintenance: false, version: 'v0.3.2' }, { id: 'installation-nova-workshop-staging', productName: 'Digvation Workshop', installationName: 'Nova Workshop Staging', environment: 'STAGING', health: 'DEGRADED', serviceSummary: 'One background service is degraded', lastObservedAt: '2026-09-07T01:18:00Z', maintenance: true, version: 'v0.3.2' }], incidents: [{ id: 'client-incident-nova-worker', title: 'Background processing is delayed', summary: 'Some background processing may take longer than usual. Digvation is investigating.', severity: 'HIGH', status: 'ACKNOWLEDGED', systemId: 'installation-nova-workshop-staging', detectedAt: '2026-09-07T01:18:00Z' }] },
  [CLIENT_B_CONTEXT.clientId]: { context: CLIENT_B_CONTEXT, systems: [{ id: 'installation-poseidon-web-production', productName: 'Poseidon Filter Website', installationName: 'Poseidon Production', environment: 'PRODUCTION', applicationUrl: 'https://poseidonfilter.id', health: 'HEALTHY', serviceSummary: 'All client-visible services healthy', lastObservedAt: '2026-09-07T01:22:00Z', maintenance: false, version: 'v1.4.0' }], incidents: [] },
};
const clone = <T,>(value: T): T => JSON.parse(JSON.stringify(value)) as T;
export function createMockClientMonitoringDataSource(): ClientMonitoringDataSource { return { async getMonitoring(context) { const record = RECORDS[context.clientId]; if (!record) return { context: clone(context), systems: [], incidents: [] }; const permitted = new Set(context.permittedInstallationIds); const systems = record.systems.filter((system) => permitted.has(system.id)); const systemIds = new Set(systems.map((system) => system.id)); return { context: clone(record.context), systems: clone(systems), incidents: clone(record.incidents.filter((incident) => systemIds.has(incident.systemId))) }; } }; }
