import type { ClientProductCommercialSummary, InitialSubscriptionInput, ProductConfiguration, RenewalTermInput, Subscription, SubscriptionTerm } from '../types/subscription';
export interface SubscriptionDataSource { getClientCommercialSummary(clientId: string): Promise<ClientProductCommercialSummary[]>; createInitialSubscription(input: InitialSubscriptionInput): Promise<Subscription>; addRenewalTerm(subscriptionId: string, input: RenewalTermInput): Promise<SubscriptionTerm>; updateProductConfiguration(clientProductId: string, configurations: ProductConfiguration[]): Promise<ProductConfiguration[]>; }
