import { useQuery } from '@tanstack/react-query'; import { resolveRuntimeDataSource } from '../data/resolve-runtime-data-source'; const dataSource = resolveRuntimeDataSource(); export function useRuntimeData() { return useQuery({ queryKey: ['runtime', 'data'], queryFn: () => dataSource.getRuntimeData() }); } export function useInstallationRuntime(installationId: string) { return useQuery({ queryKey: ['runtime', 'installation', installationId], queryFn: () => dataSource.getInstallationRuntime(installationId), enabled: Boolean(installationId) }); }
